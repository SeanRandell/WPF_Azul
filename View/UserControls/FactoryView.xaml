<UserControl x:Class="WPF_Azul.View.UserControls.FactoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Azul.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="125" d:DesignWidth="125">
    <Grid>
        <!--<Ellipse Stroke="Black">
        </Ellipse>-->

        <!--<Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
        </Grid>
        <Rectangle x:Name="rectangleUI" 
         Height="50" Width="50"
         RadiusX="5" RadiusY="5" 
         
         >
            <Rectangle.Stroke>
                <SolidColorBrush Color="Black"></SolidColorBrush>
            </Rectangle.Stroke>

            <Rectangle.Fill>
                <SolidColorBrush Color="{Binding Path=BackgroundColour}" Opacity="0.15"/>

            </Rectangle.Fill>
        </Rectangle>-->
        <ItemsControl ItemsSource="{Binding FactoryTiles}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <UniformGrid Columns="1" HorizontalAlignment="Stretch" Width="100" Height="100"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ItemsControl ItemsSource="{Binding}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Rows="1"/>
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <local:TileView TileColour="{Binding TileTypeColour}"/>
      
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
