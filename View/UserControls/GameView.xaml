<UserControl x:Class="WPF_Azul.View.UserControls.GameView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Azul.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="1000" d:DesignWidth="1000">
    <UserControl.Resources>
        <!--<DataTemplate DataType="TileType">
            -->
        <!--<local:tileslot/>-->
        <!--
            <Button Content="{Binding TileType}"/>
        </DataTemplate>-->
    </UserControl.Resources>

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>

        <!--Main Menu Button-->
        <Grid Grid.Row="0">
            <StackPanel Margin="0, 25, 0, 0" HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="Game View" 
                   FontWeight="Bold" FontSize="18"
                   HorizontalAlignment="Center" VerticalAlignment="Center" />
                <Button Margin="2" Content="Main Menu" Command="{Binding MainMenuCommand}"/>
            </StackPanel>
        </Grid>

        <!-- Grid Setup-->
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3.5*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="3.5*"/>
            </Grid.ColumnDefinitions>

            <!-- Grid Section Labels-->
            <TextBlock Grid.Row="1" Grid.Column="0" Text="Player 1"/>
            <TextBlock Grid.Row="1" Grid.Column="1" Text="Factories"/>
            <TextBlock Grid.Row="1" Grid.Column="2" Text="Center Factory"/>
            <TextBlock Grid.Row="1" Grid.Column="3" Text="Player 2"/>

            <!-- Player 1 Production Tiles with nullable test-->
            

            <!-- Player 1 Wall Tiles-->
            <ItemsControl ItemsSource="{Binding WallPattern}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="1" HorizontalAlignment="Stretch" Width="250" Height="250"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ItemsControl ItemsSource="{Binding}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Rows="1"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <local:TileSlot BackgroundColour="{Binding}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <!-- Test Factory View-->
            <local:FactoryView Grid.Row="1" Grid.Column="1"/>

            <!-- Multiple Factories Generation Test-->
            <!--<ItemsControl ItemsSource="{Binding Factories}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <GroupBox Header="Factory">
                            <ItemsControl ItemsSource="{Binding FactoryTiles}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Vertical" />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>

                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <ContentControl Content="{Binding}" />
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </GroupBox>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>-->


        </Grid>
    </Grid>
</UserControl>
