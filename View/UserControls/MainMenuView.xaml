<UserControl x:Class="WPF_Azul.View.UserControls.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Azul.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock Text="Main Menu" 
                   FontWeight="Bold" FontSize="18"
                   HorizontalAlignment="Center" VerticalAlignment="Center" />
        <StackPanel Margin="0, 25, 0, 0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Margin="2" Content="Start" Command="{Binding StartGameCommand}"/>
            <Button Margin="2" Content="Quit" Command="{Binding QuitCommand}"/>
        </StackPanel>
    </Grid>
</UserControl>
