<UserControl x:Class="WPF_Azul.View.UserControls.TileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_Azul.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="100"
             AllowDrop="True">
    <Grid>
        <Rectangle x:Name="TileViewUI" 
         Height="50" Width="50"
         RadiusX="5" RadiusY="5" 
         StrokeThickness="5"
         >
            <Rectangle.Stroke>
                <SolidColorBrush Color="{Binding Path=TileColour}" ></SolidColorBrush>
            </Rectangle.Stroke>

            <Rectangle.Fill>
                <SolidColorBrush Color="{Binding Path=TileColour}" Opacity=".75"/>
            </Rectangle.Fill>
        </Rectangle>

    </Grid>
</UserControl>
